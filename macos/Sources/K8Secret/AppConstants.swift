import Foundation

enum AppConstants {
    static let version = "0.5.2"
    static let appName = "K8Secret"

    // Auto-update: Azure Blob Storage manifest URL
    // Update this to your Azure storage account URL
    static let updateManifestURL = "https://orbitalk8releases.blob.core.windows.net/k8secret-releases/latest.json"
}
